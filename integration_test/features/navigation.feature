# language: zh-CN
功能: 基础导航
  作为用户
  我希望能在各个页面之间切换
  以便查看不同的功能模块

  场景: 启动应用默认显示自选页
    假如 应用已启动
    那么 底部导航栏的"自选"Tab应该被选中
    并且 页面应该显示自选列表区域

  场景: 切换到全市场页
    假如 应用已启动
    当 我点击底部导航栏的"全市场"Tab
    那么 底部导航栏的"全市场"Tab应该被选中
    并且 页面应该显示全市场列表区域

  场景: 切换到行业页
    假如 应用已启动
    当 我点击底部导航栏的"行业"Tab
    那么 底部导航栏的"行业"Tab应该被选中
    并且 页面应该显示行业列表区域

  场景: 切换到回踩页
    假如 应用已启动
    当 我点击底部导航栏的"回踩"Tab
    那么 底部导航栏的"回踩"Tab应该被选中
    并且 页面应该显示回踩列表区域

  场景: 自选页内切换到持仓Tab
    假如 应用已启动
    并且 当前在自选页
    当 我点击持仓Tab
    那么 应该显示持仓列表区域
    并且 应该显示从截图导入按钮

  场景: 自选页内切换回自选Tab
    假如 应用已启动
    并且 当前在自选页
    当 我点击持仓Tab
    并且 我点击自选Tab
    那么 应该显示自选列表区域
    并且 应该显示添加股票输入框
